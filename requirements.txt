fastapi==0.136.3
uvicorn[standard]==0.49.0
apscheduler==3.10.4
httpx==0.28.1
# Trade Republic is CSV-import-only (no automated API — TR blocks unofficial
# logins and it risks an account ban), so no pytr/playwright dependency is needed.
python-dotenv==1.2.2
python-multipart==0.0.31
webauthn==2.7.1
argon2-cffi==25.1.0
