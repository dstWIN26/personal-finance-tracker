fastapi==0.136.3
uvicorn[standard]==0.49.0
apscheduler==3.10.4
httpx==0.28.1
# pytr 0.4.9: Trade Republic now requires its modern web-login flow + an AWS WAF
# anti-bot token (0.4.1's device-keyfile login is rejected with CLIENT_VERSION_OUTDATED).
# Pulls in playwright + curl_cffi (transitively). We use the keyless `awswaf` token
# mode, so NO browser binary is needed — `pip install` alone suffices (no chromium).
pytr==0.4.9
python-dotenv==1.2.2
python-multipart==0.0.31
webauthn==2.7.1
argon2-cffi==25.1.0
