# Automatic HTTPS via Let's Encrypt. Caddy obtains + renews the cert for $DOMAIN
# and reverse-proxies to the app container. HSTS + secure headers are set by the
# app itself (SecurityHeadersMiddleware).
{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:8000
}
